#include "main.h"

/**
 * mul - multiplies two characters
 * @a: first chatacter to be multiplied
 * @b: second character to be multiplied
 * Return: the result of the multiplication
 */
int mul(int a, int b)
{
	return (a * b);
}
