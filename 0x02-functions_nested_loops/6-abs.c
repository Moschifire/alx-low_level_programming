#include "main.h"
/**
 * _abs - prints the absolute value of an integer
 * @i: i is tge integer to be converted
 * Return: absolute value of the input
 */
int _abs(int i)
{
	if (i < 0)
		i = i * -1;
	return (i);
}
