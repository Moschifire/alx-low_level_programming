#include<stdio.h>

/**
 * main -this is a desriptuon
 * Return: return 0
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art, ");
	fprintf(stderr, "Anything")
	return (0);
}
